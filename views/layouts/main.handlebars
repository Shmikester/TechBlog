<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

<body class="has-background-info-light">
    <header>
        <section class="hero">
            <div class="hero-body has-background-primary">
                <nav class="navbar is-max-desktop">
                    <a href="/" class="navbar-item title mb-0">Tech Blog</a>
                    <div class="navbar-brand">
                        <a role="button" class="navbarFunction" aria-label="menu" aria-expanded="false"
                            data-target="navbarMenu">
                            <span aria-hidden="true"></span>
                            <span aria-hidden="true"></span>
                            <span aria-hidden="true"></span>
                        </a>
                    </div>
                    <div id="navbarMenu" class="navbar-menu">
                        <div class="navbar-start">
                            {{#if dashboardLoggedIn}}
                            <a href="/dashboard/addPost" class="navbar-item mt-3 button is-success"><i
                                    class="fas fa-plus mr-2"></i>Add Post</a>
                            {{/if}}
                        </div>
                        <div class="navbar-end">
                            <div class="navbar-item">
                                <div class="buttons">
                                    {{#if loggedIn}}
                                    {{#if dashboardLoggedIn}}
                                    <a href="/dashboard/profile" class="button is-primary">{{dashboardUser}}</a>
                                    {{else}}
                                    <a href="/dashboard" class="button is-primary">My Posts</a>
                                    {{/if}}
                                    <a id="logoutUser" class="button is-danger">Log out</a>
                                    {{else}}
                                    <a href="/signup" class="button is-secondary"><strong>Sign up</strong></a>
                                    <a href="/login" class="button is-info">Log in</a>
                                    {{/if}}
                                </div>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
        </section>
    </header>
    <main class="section">
        {{{ body }}}
    </main>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    <script src="/javascript/navFunctions.js"></script>
</body>

</html>